services:
  backend:
    build: .
    container_name: backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    environment:
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped